# Optional Environment Configuration
# Copy this file to .env if you want to customize local settings

# Server Port (Default is 5000 locally; Render sets this automatically)
PORT=5000

# Server Host (Default: 0.0.0.0 for cloud compatibility)
HOST=0.0.0.0

# Arduino Baud Rate
BAUD_RATE=9600
